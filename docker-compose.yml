version: '3.7'

services:
    web:
        build: .
        command: python /code/manage.py runserver 0.0.0.0:8000
        # command: gunicorn lets_study_japanese.wsgi -b 0.0.0.0:8000
        environment: 
            - ENVIRONMENT=development
            - SECRET_KEY=i+invoxggkx_6m=mo8b67yavignu$$ld4r3uv31r^ow81*3bpmr
            - DEBUG=1
        volumes:
            - .:/code
        ports:
            - 8000:8000
        depends_on:
            - db
    db:
        image: postgres:11
        volumes:
            - postgres_data:/var/lib/postgresql/data/
        environment:
            - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
    postgres_data: